language: node_js
node_js:
- 8.9.0
cache:
  directories:
  - node_modules
before_install:
- npm install hexo-cli -g
install:
- npm install
script:
- hexo clean
- hexo generate
after_script:
  - cd ./public
  - git init
  - git config user.name "weipengjun"
  - git config user.email "835500598@qq.com"
  - git add .
  - git commit -m "TravisCI Blog 自动部署"
  # Github Pages
  - git push --force --quiet "https://${CI_TOKEN}@${GH_REF}" master:master 
  # Coding Pages
  # - git push --force --quiet "https://coding用户名:${CO_TOKEN}@${CO_REF}" master:master

branches:
  only:
  - next

env:
 global:
   # Github Pages
   - GH_REF: github.com/weipengjun/weipengjun.github.io  这里替换你的github仓库地址
   # Coding Pages
  #  - CO_REF: git.coding.net/weipengjun/weipengjun.coding.me.git  这里替换你的coding仓库地址
